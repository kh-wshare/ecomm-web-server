export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Merchant Commerce Hub",
  description:
    "Manage products, inventory, orders, payments, and storefront channels.",
};
